# FloodPredicter
Flood Risk Predictor is a research-grade image and video flood analysis system using FastAPI and OpenCV. It detects water bodies, suppresses false positives, generates multi-zone heatmaps, explains risk levels, and supports GPU (CUDA) acceleration with CPU fallback.
